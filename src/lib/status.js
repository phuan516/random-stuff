const statusColor = {
  Ready: "#454440",
  Done: "#008357",
  Working: "#ffa300",
  Archived: "#ff92c4",
};

const allStatus = Object.keys(statusColor);

export { statusColor, allStatus };
